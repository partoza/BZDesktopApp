@page "/categories"
@using System.Globalization

<AuthGuard>
    <div class="grid grid-cols-1 lg:grid-cols-4 md:grid-cols-2 gap-5">

        <!-- Total Categories Card -->
        <div class="bg-white rounded-xl shadow-sm p-5 border border-gray-200 hover:shadow-md transition-shadow">
            <div class="flex items-center gap-3 mb-1">
                <div class="w-12 h-12 rounded-full bg-primary/10 flex items-center justify-center">
                    <div class="w-8 h-8 bg-gradient-to-r from-emerald-600 to-emerald-500 flex items-center justify-center"
                         style="clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%)">
                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-5 h-5 text-white">
                            <path d="M19.5 21a3 3 0 003-3v-4.5a3 3 0 00-3-3h-15a3 3 0 00-3 3V18a3 3 0 003 3h15zM1.5 10.146V6a3 3 0 013-3h5.379a2.25 2.25 0 011.59.659l2.122 2.121c.14.141.331.22.53.22H19.5a3 3 0 013 3v1.146A4.483 4.483 0 0019.5 9h-15a4.483 4.483 0 00-3 1.146z" />
                        </svg>
                    </div>
                </div>
                <h2 class="text-base font-medium text-gray-700">Total Categories</h2>
            </div>

            <div class="ps-14">
                <h3 class="text-2xl font-semibold text-gray-900">@categories.Count</h3>
                <p class="text-xs text-gray-500 mt-1">As of today</p>

                <div class="flex items-center gap-2 mt-2">
                    <div class="flex items-center text-green-600 bg-green-400/10 px-2 py-1 rounded text-xs font-medium">
                        <span>+ @(GetActivePercentage())% Active</span>
                    </div>
                    <div class="flex items-center text-red-600 bg-red-400/10 px-2 py-1 text-xs font-medium">
                        <span>- @(GetInactivePercentage())% Inactive</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- No. of Subcategories Card -->
        <div class="bg-white rounded-xl shadow-sm p-5 border border-gray-200 hover:shadow-md transition-shadow">
            <div class="flex items-center gap-3 mb-1">
                <div class="w-12 h-12 rounded-full bg-primary/10 flex items-center justify-center">
                    <div class="w-8 h-8 bg-gradient-to-r from-emerald-600 to-emerald-500 flex items-center justify-center"
                         style="clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%)">
                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-5 h-5 text-white">
                            <path fill-rule="evenodd" d="M4.848 2.771A49.144 49.144 0 0112 2.25c2.43 0 4.817.178 7.152.52 1.978.292 3.348 2.024 3.348 3.97v6.02c0 1.946-1.37 3.678-3.348 3.97a48.901 48.901 0 01-3.476.383.39.39 0 00-.297.17l-2.755 4.133a.75.75 0 01-1.248 0l-2.755-4.133a.39.39 0 00-.297-.17 48.9 48.9 0 01-3.476-.384c-1.978-.29-3.348-2.024-3.348-3.97V6.741c0-1.946 1.37-3.68 3.348-3.97zM6.75 8.25a.75.75 0 01.75-.75h9a.75.75 0 010 1.5h-9a.75.75 0 01-.75-.75zm.75 2.25a.75.75 0 000 1.5H12a.75.75 0 000-1.5H7.5z" clip-rule="evenodd" />
                        </svg>
                    </div>
                </div>
                <h2 class="text-base font-medium text-gray-700">No. of Subcategories</h2>
            </div>

            <div class="ps-14">
                <h3 class="text-2xl font-semibold text-gray-900">@totalSubcategories</h3>
                <p class="text-xs text-gray-500 mt-1">Total Subcategories</p>

                <div class="flex items-center gap-2 mt-2">
                    <div class="flex items-center text-blue-600 bg-blue-400/10 px-2 py-1 rounded text-xs font-medium">
                        <span>@(categories.Count(c => c.CategoryType == "Product")) Product</span>
                    </div>
                    <div class="flex items-center text-purple-600 bg-purple-400/10 px-2 py-1 text-xs font-medium">
                        <span>@(categories.Count(c => c.CategoryType == "Service")) Service</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Active Categories Card -->
        <div class="bg-white rounded-xl shadow-sm p-5 border border-gray-200 hover:shadow-md transition-shadow">
            <div class="flex items-center gap-3 mb-1">
                <div class="w-12 h-12 rounded-full bg-primary/10 flex items-center justify-center">
                    <div class="w-8 h-8 bg-gradient-to-r from-emerald-600 to-emerald-500 flex items-center justify-center"
                         style="clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%)">
                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-5 h-5 text-white">
                            <path fill-rule="evenodd" d="M2.25 12c0-5.385 4.365-9.75 9.75-9.75s9.75 4.365 9.75 9.75-4.365 9.75-9.75 9.75S2.25 17.385 2.25 12zm13.36-1.814a.75.75 0 10-1.22-.872l-3.236 4.53L9.53 12.22a.75.75 0 00-1.06 1.06l2.25 2.25a.75.75 0 001.14-.094l3.75-5.25z" clip-rule="evenodd" />
                        </svg>
                    </div>
                </div>
                <h2 class="text-base font-medium text-gray-700">Active Categories</h2>
            </div>

            <div class="ps-14">
                <h3 class="text-2xl font-semibold text-gray-900">@activeCategories</h3>
                <p class="text-xs text-gray-500 mt-1">Currently Active</p>

                <div class="flex items-center gap-2 mt-2">
                    <div class="flex items-center text-green-600 bg-green-400/10 px-2 py-1 rounded text-xs font-medium">
                        <span>@(categories.Count(c => c.Status == "Active" && c.ParentId == null)) Main</span>
                    </div>
                    <div class="flex items-center text-blue-600 bg-blue-400/10 px-2 py-1 text-xs font-medium">
                        <span>@(categories.Count(c => c.Status == "Active" && c.ParentId != null)) Sub</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Inactive Categories Card -->
        <div class="bg-white rounded-xl shadow-sm p-5 border border-gray-200 hover:shadow-md transition-shadow">
            <div class="flex items-center gap-3 mb-1">
                <div class="w-12 h-12 rounded-full bg-primary/10 flex items-center justify-center">
                    <div class="w-8 h-8 bg-gradient-to-r from-emerald-600 to-emerald-500 flex items-center justify-center"
                         style="clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%)">
                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-5 h-5 text-white">
                            <path fill-rule="evenodd" d="M12 2.25c-5.385 0-9.75 4.365-9.75 9.75s4.365 9.75 9.75 9.75 9.75-4.365 9.75-9.75S17.385 2.25 12 2.25zm-1.72 6.97a.75.75 0 10-1.06 1.06L10.94 12l-1.72 1.72a.75.75 0 101.06 1.06L12 13.06l1.72 1.72a.75.75 0 101.06-1.06L13.06 12l1.72-1.72a.75.75 0 10-1.06-1.06L12 10.94l-1.72-1.72z" clip-rule="evenodd" />
                        </svg>
                    </div>
                </div>
                <h2 class="text-base font-medium text-gray-700">Inactive Categories</h2>
            </div>

            <div class="ps-14">
                <h3 class="text-2xl font-semibold text-gray-900">@inactiveCategories</h3>
                <p class="text-xs text-gray-500 mt-1">Currently Inactive</p>

                <div class="flex items-center gap-2 mt-2">
                    <div class="flex items-center text-red-600 bg-red-400/10 px-2 py-1 rounded text-xs font-medium">
                        <span>@(categories.Count(c => c.Status == "Inactive" && c.ParentId == null)) Main</span>
                    </div>
                    <div class="flex items-center text-orange-600 bg-orange-400/10 px-2 py-1 text-xs font-medium">
                        <span>@(categories.Count(c => c.Status == "Inactive" && c.ParentId != null)) Sub</span>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Category Table -->
    <div class="bg-white rounded-xl shadow-sm overflow-hidden border border-gray-200 mt-5">
        <div class="bg-white dark:bg-gray-800 rounded-t-xl shadow-sm border border-gray-100 dark:border-gray-700 p-6">
            <!-- Header Section -->
            <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-6 mb-6">
                <!-- Title Section -->
                <div class="flex items-center gap-3">
                    <div class="p-2 bg-primary/10 rounded-full">
                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="size-6 text-primary">
                            <path d="M3.375 3C2.339 3 1.5 3.84 1.5 4.875v.75c0 1.036.84 1.875 1.875 1.875h17.25c1.035 0 1.875-.84 1.875-1.875v-.75C22.5 3.839 21.66 3 20.625 3H3.375Z" />
                            <path fill-rule="evenodd" d="m3.087 9 .54 9.176A3 3 0 0 0 6.62 21h10.757a3 3 0 0 0 2.995-2.824L20.913 9H3.087Zm6.163 3.75A.75.75 0 0 1 10 12h4a.75.75 0 0 1 0 1.5h-4a.75.75 0 0 1-.75-.75Z" clip-rule="evenodd" />
                        </svg>


                    </div>
                    <div>
                        <h2 class="text-xl font-semibold text-primary dark:text-white">Category List</h2>
                        <p class="text-sm text-gray-500 dark:text-gray-400 mt-1">Manage product categories efficiently</p>
                    </div>
                </div>

                <!-- Search Section -->
                <div class="flex-1 max-w-xl">
                    <form class="w-full">
                        <div class="relative">
                            <div class="absolute inset-y-0 left-0 flex items-center pl-4 pointer-events-none">
                                <svg class="w-4 h-4 text-gray-500 dark:text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m21 21-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                                </svg>
                            </div>
                            <input type="search"
                                   class="block w-full pl-12 pr-28 py-3.5 text-sm text-gray-900 bg-white dark:bg-gray-700 border border-gray-200 dark:border-gray-600 rounded-xl focus:ring-2 focus:ring-primary/20 focus:border-primary transition-all duration-200 dark:text-white dark:placeholder-gray-400"
                                   placeholder="Search categories ..."
                                   required />
                            <button type="submit"
                                    class="absolute right-2 top-1/2 transform -translate-y-1/2 px-4 py-2 text-sm font-medium text-white bg-primary hover:bg-emerald-600 rounded-lg transition-all duration-200 focus:ring-2 focus:ring-primary/20 flex items-center gap-2 cursor-pointer">
                                Search
                            </button>
                        </div>
                    </form>
                </div>

                <!-- Action Buttons -->
                <div class="flex items-center gap-3">
                    <!-- Add Employee Button -->
                    <button type="button"
                            data-modal-target="addCategoryModal"
                            data-modal-toggle="addCategoryModal"
                            class="px-4 py-3 bg-primary hover:bg-emerald-600 text-white rounded-xl text-sm font-medium transition-all duration-200 shadow-sm hover:shadow-md focus:ring-2 focus:ring-primary/20 flex items-center gap-2 cursor-pointer">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
                        </svg>
                        Add Categories
                    </button>

                    <!-- Export Button -->
                    <a href="#"
                       class="px-4 py-3 bg-white dark:bg-gray-700 border border-gray-200 dark:border-gray-600 rounded-xl text-sm text-gray-700 dark:text-gray-300 font-medium transition-all duration-200 shadow-sm hover:shadow-md hover:bg-gray-50 dark:hover:bg-gray-600 focus:ring-2 focus:ring-gray-200 dark:focus:ring-gray-600 flex items-center gap-2">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                        </svg>
                        Export
                    </a>
                </div>
            </div>

            <!-- Filters Section -->
            <div class="bg-gray-50 dark:bg-gray-700/50 rounded-xl border border-gray-200 dark:border-gray-600 p-5">
                <!-- Filters Header -->
                <div class="flex items-center justify-between mb-4">
                    <div class="flex items-center gap-3">
                        <svg class="w-5 h-5 text-gray-600 dark:text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 4a1 1 0 011-1h16a1 1 0 011 1v2.586a1 1 0 01-.293.707l-6.414 6.414a1 1 0 00-.293.707V17l-4 4v-6.586a1 1 0 00-.293-.707L3.293 7.207A1 1 0 013 6.5V4z" />
                        </svg>
                        <h3 class="font-semibold text-gray-900 dark:text-white">Filter Options</h3>
                    </div>
                    <span class="text-xs text-gray-500 dark:text-gray-400 px-2 py-1 bg-white dark:bg-gray-600 rounded-lg">2 filters</span>
                </div>

                <!-- Filters Form -->
                <form id="filtersForm" method="GET" class="flex flex-col lg:flex-row lg:items-end justify-between gap-4">
                    <!-- Filter Controls -->
                    <div class="flex flex-col sm:flex-row gap-4 flex-1">
                        <!-- Status Filter -->
                        <div class="flex flex-col gap-2">
                            <label for="categoriesTypeSelect" class="text-xs font-medium text-gray-600 dark:text-gray-400 uppercase tracking-wide">
                                Category Type
                            </label>
                            <div class="relative">
                                <select id="categoriesTypeSelect"
                                        class="w-full min-w-[200px] appearance-none text-gray-900 bg-white dark:bg-gray-600 border border-gray-300 dark:border-gray-500 rounded-xl py-3 px-4 pr-10 text-sm font-medium focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary transition-all duration-200 cursor-pointer shadow-sm">
                                    <option value="all">Main Categories</option>
                                    <option value="active">Sub Categories</option>
                                </select>
                            </div>
                        </div>

                        <!-- Role Filter -->
                        <div class="flex flex-col gap-2">
                            <label for="catgoriesStatusSelect" class="text-xs font-medium text-gray-600 dark:text-gray-400 uppercase tracking-wide">
                                Status
                            </label>
                            <div class="relative">
                                <select id="catgoriesStatusSelect"
                                        class="w-full min-w-[200px] appearance-none text-gray-900 bg-white dark:bg-gray-600 border border-gray-300 dark:border-gray-500 rounded-xl py-3 px-4 pr-10 text-sm font-medium focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary transition-all duration-200 cursor-pointer shadow-sm">
                                    <option value="all">Active</option>
                                    <option value="superadmin">Inactive</option>
                                </select>

                            </div>
                        </div>

                        <!-- Role Filter -->
                        <div class="flex flex-col gap-2">
                            <label for="categoriesBranchSelect" class="text-xs font-medium text-gray-600 dark:text-gray-400 uppercase tracking-wide">
                                Branch
                            </label>
                            <div class="relative">
                                <select id="categoriesRolesSelect"
                                        class="w-full min-w-[200px] appearance-none text-gray-900 bg-white dark:bg-gray-600 border border-gray-300 dark:border-gray-500 rounded-xl py-3 px-4 pr-10 text-sm font-medium focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary transition-all duration-200 cursor-pointer shadow-sm">
                                    <option value="all">All Branch</option>
                                    <option value="superadmin">Davao</option>
                                    <option value="admin">Kidapawan </option>
                                    <option value="employee">Gensan</option>
                                </select>

                            </div>
                        </div>
                    </div>

                    <!-- Action Buttons -->
                    <div class="flex items-center gap-3 pt-2">
                        <!-- Filter Button -->
                        <button type="submit"
                                class="px-5 py-3 bg-primary hover:bg-emerald-600 text-white rounded-xl text-sm font-medium transition-all duration-200 shadow-sm hover:shadow-md focus:ring-2 focus:ring-primary/20 flex items-center gap-2 cursor-pointer">
                            Apply Filters
                        </button>

                        <!-- Reset Button -->
                        <button id="resetFilters" type="button"
                                class="px-4 py-3 bg-white dark:bg-gray-600 border border-gray-300 dark:border-gray-500 rounded-xl text-sm text-gray-700 dark:text-gray-300 font-medium transition-all duration-200 shadow-sm hover:shadow-md hover:bg-gray-50 dark:hover:bg-gray-500 focus:ring-2 focus:ring-gray-200 dark:focus:ring-gray-600 flex items-center gap-2 cursor-pointer">
                            Reset
                        </button>
                    </div>
                </form>
            </div>
        </div>


        <div class="overflow-hidden">
            <table class="min-w-full divide-y divide-gray-200 text-sm text-left">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left font-medium text-gray-700">Category Name</th>
                        <th class="px-6 py-3 text-left font-medium text-gray-700">Type</th>
                        <th class="px-6 py-3 text-left font-medium text-gray-700">
                            @(viewType == "subcategories" ? "Parent Category" : "Sub-Categories")
                        </th>
                        <th class="px-6 py-3 text-left font-medium text-gray-700">Products</th>
                        <th class="px-6 py-3 text-left font-medium text-gray-700">Total Products</th>
                        <th class="px-6 py-3 text-left font-medium text-gray-700">Inventory Value</th>
                        <th class="px-6 py-3 text-left font-medium text-gray-700">Status</th>
                        <th class="px-6 py-3 text-left font-medium text-gray-700">Actions</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-100">
                    @foreach (var cat in categories)
                    {
                        <tr class="hover:bg-gray-50">
                            <td class="px-6 py-3">
                                <div class="flex items-center">
                                    <div class="w-8 h-8 rounded-full bg-primary/10 flex items-center justify-center mr-3">
                                        <svg class="w-4 h-4 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 8h14M5 8a2 2 0 110-4h14a2 2 0 110 4M5 8v10a2 2 0 002 2h10a2 2 0 002-2V8m-9 4h4"></path>
                                        </svg>
                                    </div>
                                    <div>
                                        <div class="font-medium text-gray-800">@cat.Name</div>
                                    </div>
                                </div>
                            </td>

                            <td class="px-6 py-3">
                                <span class="px-2 py-1 text-xs font-medium rounded-full bg-purple-100 text-purple-800">@cat.CategoryType</span>
                            </td>

                            <td class="px-6 py-3 text-gray-600">
                                @if (viewType == "subcategories")
                                {
                                    @(cat.ParentCategory?.Name ?? "— None —")
                                }
                                else
                                {
                                    @cat.SubcategoriesCount
                                }
                            </td>

                            <td class="px-6 py-3 text-gray-600">@cat.DirectProductsCount</td>

                            <td class="px-6 py-3 text-gray-600">@cat.TotalProductsCount</td>

                            <td class="px-6 py-3 text-gray-600">₱ @cat.InventoryValue.ToString("N2")</td>

                            <td class="px-6 py-4">
                                <span class="px-2.5 py-0.5 text-xs font-medium rounded-full @(cat.Status == "Active" ? "bg-green-100 text-green-800" : "bg-red-100 text-red-800")">
                                    @(cat.Status)
                                </span>
                            </td>

                            <td class="px-6 py-4 align-middle">
                                <div class="flex space-x-2">
                                    <!-- View Button -->
                                    <div class="relative group">
                                        <button class="text-gray-600 hover:text-primary/90 transition-colors duration-200 p-1 rounded cursor-pointer">
                                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20"
                                                 fill="currentColor">
                                                <path d="M10 12a2 2 0 100-4 2 2 0 000 4z" />
                                                <path fill-rule="evenodd"
                                                      d="M.458 10C1.732 5.943 5.522 3 10 3s8.268 2.943 9.542 7c-1.274 4.057-5.064 7-9.542 7S1.732 14.057.458 10zM14 10a4 4 0 11-8 0 4 4 0 018 0z"
                                                      clip-rule="evenodd" />
                                            </svg>
                                        </button>
                                        <div class="absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 px-2 py-1 text-xs text-white bg-primary rounded opacity-0 group-hover:opacity-100 transition-opacity duration-200 whitespace-nowrap pointer-events-none z-10">
                                            View Category
                                            <div class="absolute top-full left-1/2 transform -translate-x-1/2 border-4 border-transparent border-t-primary">
                                            </div>
                                        </div>
                                    </div>

                                    <!-- Edit Button -->
                                    <div class="relative group">
                                        <button class="text-gray-600 hover:text-primary/90 transition-colors duration-200 p-1 rounded cursor-pointer">
                                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20"
                                                 fill="currentColor">
                                                <path d="M13.586 3.586a2 2 0 112.828 2.828l-.793.793-2.828-2.828.793-.793zM11.379 5.793L3 14.172V17h2.828l8.38-8.379-2.83-2.828z" />
                                            </svg>
                                        </button>
                                        <div class="absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 px-2 py-1 text-xs text-white bg-primary rounded opacity-0 group-hover:opacity-100 transition-opacity duration-200 whitespace-nowrap pointer-events-none z-10">
                                            Edit Category
                                            <div class="absolute top-full left-1/2 transform -translate-x-1/2 border-4 border-transparent border-t-primary">
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </td>
                        </tr>
                    }

                    @* @if (())
                    {
                        <tr>
                            <td colspan="8" class="px-6 py-6 text-center text-gray-500">No records found.</td>
                        </tr>
                    }  *@
                </tbody>
            </table>
        </div>

        <!-- Pagination -->
        <div class="bg-white px-6 py-4 border-t border-gray-200">
            <div class="flex items-center justify-between">
                <div class="text-sm text-gray-700">
                    Showing <span class="font-medium">1</span> to
                    <span class="font-medium">5</span> of
                    <span class="font-medium">10</span> results
                </div>
                <div class="flex items-center justify-center gap-1">
                    <!-- First -->
                    <button class="flex items-center justify-center w-7 h-7 text-xs border border-gray-300 rounded-full hover:bg-gray-50 transition-colors"
                            title="First Page">
                        <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 19l-7-7 7-7m8 14l-7-7 7-7" />
                        </svg>
                    </button>

                    <!-- Previous -->
                    <button class="flex items-center justify-center w-7 h-7 text-xs border border-gray-300 rounded-full hover:bg-gray-50 transition-colors"
                            title="Previous">
                        <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
                        </svg>
                    </button>

                    <!-- Pages -->
                    <button class="flex items-center justify-center w-7 h-7 text-xs font-medium rounded-full bg-primary text-white border border-primary">
                        1
                    </button>
                    <button class="flex items-center justify-center w-7 h-7 text-xs border border-gray-300 rounded-full hover:bg-gray-50">
                        2
                    </button>
                    <button class="flex items-center justify-center w-7 h-7 text-xs border border-gray-300 rounded-full hover:bg-gray-50">
                        3
                    </button>
                    <span class="text-xs text-gray-400 px-1">...</span>
                    <button class="flex items-center justify-center w-7 h-7 text-xs border border-gray-300 rounded-full hover:bg-gray-50">
                        8
                    </button>

                    <!-- Next -->
                    <button class="flex items-center justify-center w-7 h-7 text-xs border border-gray-300 rounded-full hover:bg-gray-50"
                            title="Next">
                        <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                        </svg>
                    </button>

                    <!-- Last -->
                    <button class="flex items-center justify-center w-7 h-7 text-xs border border-gray-300 rounded-full hover:bg-gray-50"
                            title="Last Page">
                        <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 5l7 7-7 7M5 5l7 7-7 7" />
                        </svg>
                    </button>
                </div>
            </div>
        </div>
    </div>
    <!-- Add Category Modal -->
    <div id="addCategoryModal" data-modal-backdrop="static" tabindex="-1" aria-hidden="true"
         class="hidden overflow-y-auto overflow-x-hidden fixed top-0 right-0 left-0 z-50 justify-center items-center w-full md:inset-0 h-[calc(100%-1rem)] max-h-full bg-gray-700/70 bg-opacity-50 dark:bg-opacity-80 backdrop-blur-sm">
        <div class="relative p-4 max-w-4xl w-full">
            <!-- Changed from max-w-4xl to max-w-5xl -->
            <!-- Modal content -->
            <div class="relative bg-white rounded-lg shadow dark:bg-gray-700 border border-gray-200 dark:border-gray-600">
                <!-- Modal header -->
                <div class="flex items-center justify-between p-4 md:p-5 border-b border-gray-200 rounded-t dark:border-gray-600">
                    <div>
                        <h3 class="text-xl font-semibold text-primary dark:text-white">
                            Add New Category
                        </h3>
                        <p class="text-sm text-gray-600 dark:text-gray-400 mt-1">
                            Please fill up all the required fields to add new category.
                        </p>
                    </div>
                    <button type="button"
                            class="text-gray-400 bg-transparent hover:bg-gray-200 hover:text-gray-900 rounded-lg text-sm w-8 h-8 ms-auto inline-flex justify-center items-center dark:hover:bg-gray-600 dark:hover:text-white"
                            data-modal-hide="addCategoryModal">
                        <svg class="w-3 h-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 14">
                            <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 1 6 6m0 0 6 6M7 7l6-6M7 7l-6 6" />
                        </svg>
                        <span class="sr-only">Close modal</span>
                    </button>
                </div>

                <!-- Modal body -->
                <form id="categoryForm" enctype="multipart/form-data" class="p-4 md:p-5">
                    <div class="grid grid-cols-1 gap-6 max-h-[65vh] overflow-y-auto">
                        <div class="overflow-y-auto max-h-[60vh] space-y-4 px-4">
                            <div>
                                <label for="categoryName" class="block text-sm font-medium text-gray-700 mb-2">Category Name</label>
                                <input id="categoryName"
                                           class="w-full px-4 py-3 border border-gray-200 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all duration-200 bg-gray-50"
                                           placeholder="Electronics" />
                            </div>

                            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-2">Category Type</label>
                                    <select class="w-full px-4 py-3 border border-gray-200 rounded-xl focus:ring-2 focus:ring-blue-500 bg-white">
                                        <option value="Product">Product</option>
                                        <option value="Service">Service</option>
                                    </select>
                                </div>
                                <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-2">Parent Category</label>
                                    <select class="w-full px-4 py-3 border border-gray-200 rounded-xl focus:ring-2 focus:ring-blue-500 bg-white">
                                        <option value="">None</option>
                                        @foreach (var parent in ParentCategories)
                                        {
                                            <option value="@parent.Id">@parent.Name</option>
                                        }
                                    </select>
                                </div>
                            </div>

                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-2">Description</label>
                                <textarea rows="3"
                                               class="w-full px-4 py-3 border border-gray-200 rounded-xl focus:ring-2 focus:ring-blue-500"
                                               placeholder="Brief description of the category" />
                            </div>

                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-2">Status</label>                                    <!-- Hidden Checkbox bound to your model -->
                                <label class="inline-flex items-center mb-5 cursor-pointer">
                                    <input type="checkbox" @bind="isActive" class="sr-only peer">

                                    <div class="relative w-11 h-6 rounded-full
                @(isActive ? "bg-green-600 dark:bg-green-600" : "bg-red-400 dark:bg-red-700")
                peer-focus:outline-none peer-focus:ring-4
                @(isActive ? "peer-focus:ring-green-300 dark:peer-focus:ring-green-800" : "peer-focus:ring-red-300 dark:peer-focus:ring-red-800")
                peer-checked:after:translate-x-full rtl:peer-checked:after:-translate-x-full
                peer-checked:after:border-white
                after:content-[''] after:absolute after:top-[2px] after:start-[2px]
                after:bg-white after:border-gray-300 after:border after:rounded-full
                after:w-5 after:h-5 after:transition-all dark:border-gray-600">
                                    </div>

                                    <span class="ms-3 text-sm font-medium @(isActive ? "text-green-600 dark:text-green-400" : "text-red-600 dark:text-red-400")">
                                        @(isActive ? "Active" : "Inactive")
                                    </span>
                                </label>
                            </div>
                        </div>
                    </div>

                    <!-- Confirmation Modal -->
                    <div id="addCategoryConfirmationModal" data-modal-backdrop="static" tabindex="-1" aria-hidden="true"
                         class="hidden overflow-y-auto overflow-x-hidden fixed top-0 right-0 left-0 z-[60] justify-center items-center w-full md:inset-0 h-[calc(100%-1rem)] max-h-full bg-gray-700/70 bg-opacity-50 dark:bg-opacity-80 backdrop-blur-sm">
                        <div class="relative p-4 w-full max-w-md max-h-full">
                            <!-- Modal content -->
                            <div class="relative bg-white rounded-lg shadow dark:bg-gray-700 border border-gray-200 dark:border-gray-600">
                                <!-- Modal header -->
                                <div class="flex items-center justify-between p-4 md:p-5 border-b border-gray-200 rounded-t dark:border-gray-600">
                                    <h3 class="text-xl font-semibold text-gray-900 dark:text-white">
                                        Confirm Category Creation
                                    </h3>
                                    <button type="button"
                                            class="text-gray-400 bg-transparent hover:bg-gray-200 hover:text-gray-900 rounded-lg text-sm w-8 h-8 ms-auto inline-flex justify-center items-center dark:hover:bg-gray-600 dark:hover:text-white"
                                            data-modal-hide="addCategoryConfirmationModal">
                                        <svg class="w-3 h-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 14">
                                            <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 1 6 6m0 0 6 6M7 7l6-6M7 7l-6 6" />
                                        </svg>
                                        <span class="sr-only">Close modal</span>
                                    </button>
                                </div>
                                <!-- Modal body -->
                                <div class="p-4 md:p-5">
                                    <div class="flex items-center mb-4">
                                        <div class="flex-shrink-0">
                                            <svg class="w-6 h-6 text-primary dark:text-blue-500" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 20 20">
                                                <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 11V6m0 8h.01M19 10a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />
                                            </svg>
                                        </div>
                                        <div class="ms-3">
                                            <p class="text-sm text-gray-700 dark:text-gray-300">
                                                Are you sure you want to add this new product category? This action will update your product records.
                                            </p>
                                        </div>
                                    </div>
                                    <div class="bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-800 rounded-lg p-3 mb-4">
                                        <p class="text-xs text-blue-800 dark:text-blue-300">
                                            <strong>Note:</strong> Once created, this category will be available for product listings and can be assigned to items in your inventory.
                                        </p>
                                    </div>
                                </div>

                                <!-- Modal footer -->
                                <div class="flex items-center justify-end p-4 md:p-5 border-t border-gray-200 rounded-b dark:border-gray-600 gap-3">
                                    <button type="button" data-modal-hide="addCategoryConfirmationModal"
                                            class="py-2.5 px-5 text-sm font-medium text-gray-900 focus:outline-none bg-white rounded-lg border border-gray-200 hover:bg-gray-100 focus:z-10 focus:ring-4 focus:ring-gray-100 dark:focus:ring-gray-700 dark:bg-gray-800 dark:text-gray-400 dark:border-gray-600 dark:hover:text-white dark:hover:bg-gray-700 cursor-pointer">
                                        Cancel
                                    </button>
                                    <button type="button" id="finalCreateBtn"
                                            class="text-white bg-primary hover:bg-emerald-600 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800 cursor-pointer">
                                        Yes, Add Category
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Modal footer -->
                    <div class="flex items-center justify-end pt-4 border-t border-gray-200 rounded-b dark:border-gray-600 mt-6 gap-3">
                        <button type="button"
                                data-modal-hide="addCategoryModal"
                                class="py-2.5 px-5 text-sm font-medium text-gray-900 focus:outline-none bg-white rounded-lg border border-gray-200 hover:bg-gray-100 focus:z-10 focus:ring-4 focus:ring-gray-100 dark:focus:ring-gray-700 dark:bg-gray-800 dark:text-gray-400 dark:border-gray-600 dark:hover:text-white dark:hover:bg-gray-700 cursor-pointer">
                            Cancel
                        </button>
                        <button type="button" data-modal-target="addCategoryConfirmationModal"
                            data-modal-toggle="addCategoryConfirmationModal"
                                class="text-white bg-primary hover:bg-emerald-600 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800 cursor-pointer">
                            Add Category
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>




    <!-- Add Category Modal -->
    @if (showAddModal)
    {
        <div class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 backdrop-blur-sm">
            <div class="bg-white p-5 rounded-2xl shadow-2xl w-full max-w-4xl mx-4 border border-gray-100">
                <div class="flex items-center justify-between mb-4">
                    <div>
                        <h2 class="text-2xl font-bold text-primary">@(editingCategory == null ? "Add New Category" : "Edit Category")</h2>
                        <p class="text-sm text-gray-600 mt-1">Please fill up all the required fields to @(editingCategory == null ? "add new" : "edit") category.</p>
                    </div>
                    <button type="button" class="text-gray-400 hover:text-red-600 transition-colors">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                        </svg>
                    </button>
                </div>

                <hr class="mb-2">

                <form class="global-focus flex flex-col">

                    <!-- Scrollable body -->
                    <div class="overflow-y-auto max-h-[60vh] space-y-4 mt-4">
                        <div>
                            <label for="categoryName" class="block text-sm font-medium text-gray-700 mb-2">Category Name</label>
                            <InputText id="categoryName"
                                       class="w-full px-4 py-3 border border-gray-200 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all duration-200 bg-gray-50"
                                       placeholder="Electronics" />
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-2">Category Type</label>
                                <InputSelect @bind-Value="categoryForm.CategoryType" class="w-full px-4 py-3 border border-gray-200 rounded-xl focus:ring-2 focus:ring-blue-500 bg-white">
                                    <option value="Product">Product</option>
                                    <option value="Service">Service</option>
                                </InputSelect>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-2">Parent Category</label>
                                <InputSelect @bind-Value="categoryForm.ParentId" class="w-full px-4 py-3 border border-gray-200 rounded-xl focus:ring-2 focus:ring-blue-500 bg-white">
                                    <option value="">None</option>
                                    @foreach (var parent in ParentCategories)
                                    {
                                        <option value="@parent.Id">@parent.Name</option>
                                    }
                                </InputSelect>
                            </div>
                        </div>

                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">Description</label>
                            <InputTextArea @bind-Value="categoryForm.Description" rows="3"
                                           class="w-full px-4 py-3 border border-gray-200 rounded-xl focus:ring-2 focus:ring-blue-500"
                                           placeholder="Brief description of the category" />
                        </div>

                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">Status</label>
                            <div class="flex items-center gap-3 px-3 py-2 bg-gray-50 border border-gray-200 rounded-xl">
                                <input type="checkbox" @bind="categoryForm.IsActive"
                                       class="h-5 w-5 text-blue-600 focus:ring-blue-500 border-gray-300 rounded" />
                                <label class="text-sm text-gray-800 select-none cursor-pointer">Active</label>
                            </div>
                        </div>
                    </div>

                    <!-- Footer pinned to bottom -->
                    <div class="mt-4 pt-4 border-t flex justify-end gap-3">
                        <button type="button"
                                class="px-4 py-3 border border-gray-300 text-gray-700 rounded-xl hover:bg-gray-100 transition-colors font-medium">
                            Cancel
                        </button>
                        <button type="submit"
                                class="px-4 py-3 bg-primary hover:bg-emerald-700 text-white rounded-xl font-medium shadow-sm hover:shadow-md transition-all duration-200">
                            @(editingCategory == null ? "Create Category" : "Update Category")
                        </button>
                    </div>
                </form>
            </div>
        </div>
    }

    <!-- Confirmation Modal for Delete -->
    @if (showDeleteModal)
    {
        <div class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 backdrop-blur-sm">
            <div class="bg-white p-6 rounded-2xl shadow-2xl w-full max-w-md mx-4 border border-gray-100">
                <div class="text-center">
                    <div class="mx-auto flex items-center justify-center h-12 w-12 rounded-full bg-red-100">
                        <svg class="h-6 w-6 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L4.082 16.5c-.77.833.192 2.5 1.732 2.5z" />
                        </svg>
                    </div>
                    <h3 class="text-lg font-medium text-gray-900 mt-3">Delete Category</h3>
                    <p class="text-sm text-gray-500 mt-2">
                        Are you sure you want to delete category "@categoryToDelete?.Name"? This action cannot be undone.
                    </p>
                </div>
                <div class="mt-6 flex justify-end gap-3">
                    <button type="button"
                            class="px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50 transition-colors font-medium">
                        Cancel
                    </button>
                    <button type="button" 
                            class="px-4 py-2 bg-red-600 hover:bg-red-700 text-white rounded-lg font-medium transition-colors">
                        Delete
                    </button>
                </div>
            </div>
        </div>
    }
</AuthGuard>

@code {
    private bool isActive { get; set; } = true; // initial state
    private List<Category> categories = new();
    private bool showAddModal = false;
    private bool showDeleteModal = false;
    private Category? editingCategory = null;
    private Category? categoryToDelete = null;
    private CategoryForm categoryForm = new();
    private string searchTerm = string.Empty;
    private string viewType = "categories";
    private string statusFilter = "";
    private int currentPage = 1;
    private int pageSize = 10;

    // Computed properties
    private int totalSubcategories => categories.Count(c => c.ParentId.HasValue);
    private int activeCategories => categories.Count(c => c.Status == "Active");
    private int inactiveCategories => categories.Count(c => c.Status == "Inactive");

    private List<Category> ParentCategories => categories.Where(c => c.ParentId == null).ToList();

    protected override void OnInitialized()
    {
        LoadSampleData();
    }

    private void LoadSampleData()
    {
        categories = new List<Category>
        {
            new Category { Id = 1, Name = "Electronics", CategoryType = "Product", Status = "Active",
                         DirectProductsCount = 15, TotalProductsCount = 45, InventoryValue = 125000.50m,
                         SubcategoriesCount = 3, CreatedAt = DateTime.Now },

            new Category { Id = 2, Name = "Mobile Phones", CategoryType = "Product", ParentId = 1, Status = "Active",
                         DirectProductsCount = 8, TotalProductsCount = 8, InventoryValue = 75000.25m,
                         SubcategoriesCount = 0, CreatedAt = DateTime.Now },

            new Category { Id = 3, Name = "Laptops", CategoryType = "Product", ParentId = 1, Status = "Active",
                         DirectProductsCount = 7, TotalProductsCount = 7, InventoryValue = 50000.75m,
                         SubcategoriesCount = 0, CreatedAt = DateTime.Now },

            new Category { Id = 4, Name = "Services", CategoryType = "Service", Status = "Active",
                         DirectProductsCount = 0, TotalProductsCount = 12, InventoryValue = 0,
                         SubcategoriesCount = 2, CreatedAt = DateTime.Now },

            new Category { Id = 5, Name = "Repair Services", CategoryType = "Service", ParentId = 4, Status = "Inactive",
                         DirectProductsCount = 0, TotalProductsCount = 5, InventoryValue = 0,
                         SubcategoriesCount = 0, CreatedAt = DateTime.Now }
        };

        // Set parent category references
        foreach (var category in categories)
        {
            if (category.ParentId.HasValue)
            {
                category.ParentCategory = categories.FirstOrDefault(c => c.Id == category.ParentId.Value);
            }
        }
    }

    private double GetActivePercentage()
    {
        return categories.Count > 0 ? Math.Round((activeCategories / (double)categories.Count) * 100, 0) : 0;
    }

    private double GetInactivePercentage()
    {
        return categories.Count > 0 ? Math.Round((inactiveCategories / (double)categories.Count) * 100, 0) : 0;
    }

    // Model Classes
    public class Category
    {
        public int Id { get; set; }
        public string Name { get; set; } = string.Empty;
        public string CategoryType { get; set; } = "Product";
        public int? ParentId { get; set; }
        public Category? ParentCategory { get; set; }
        public string Description { get; set; } = string.Empty;
        public string Status { get; set; } = "Active";
        public int DirectProductsCount { get; set; }
        public int TotalProductsCount { get; set; }
        public decimal InventoryValue { get; set; }
        public int SubcategoriesCount { get; set; }
        public DateTime CreatedAt { get; set; }
    }

    public class CategoryForm
    {
        public string Name { get; set; } = string.Empty;
        public string CategoryType { get; set; } = "Product";
        public int? ParentId { get; set; }
        public string Description { get; set; } = string.Empty;
        public bool IsActive { get; set; } = true;
    }
}